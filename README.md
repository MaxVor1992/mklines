# Парсер поисковой выдачи Яндекс и Google

**Веб-инструмент для анализа позиций сайтов в поисковой выдаче Яндекс и Google**

Проект расположен по адресу: [https://mklines.ru/parser/index](https://mklines.ru/parser/index)

---

## 📋 Описание

Инструмент предназначен для парсинга поисковой выдачи Яндекс и Google с целью анализа сайтов, находящихся на 1, 2, 3 страницах результатов поиска. Позволяет отслеживать позиции сайтов по ключевым словам, проводить кластеризацию запросов и экспортировать результаты.

---

## 🖥️ Серверная среда

### Хостинг
- **Провайдер**: Beget (виртуальный сервер)
- **Сервер**: hyper.beget.com

### Технические характеристики сервера
| Компонент | Версия |
|-----------|--------|
| ОС | Ubuntu Server Edition |
| Web-сервер | Apache 2.4.63 + Nginx 1.21.1 |
| СУБД | MySQL 5.7.21-20-beget |
| PHP | 7.1 |
| Perl | 5.34.0 |
| Python | 3.8 |
| Flask | 2.2.2 |

### Ресурсы сервера
- **CPU**: 96 × Intel(R) Xeon(R) Gold 6240R CPU @ 2.40GHz
- **ОЗУ**: 151330/644091 МБ свободно
- **Нагрузка**: 38.55 load average

---

## 🛠️ Технологии

### Backend
- **Python 3.8** - основной язык разработки
- **Flask 2.2.2** - веб-фреймворк
- **Flask-SQLAlchemy 3.0.2** - ORM для работы с БД
- **Flask-Login 0.6.2** - управление сессиями пользователей
- **Flask-Admin 1.6.0** - административная панель
- **Flask-Security 3.0.0** - система безопасности и ролей
- **SQLite** - база данных (db.sqlite)

### Парсинг и работа с данными
- **Requests 2.25.1** - HTTP-запросы
- **BeautifulSoup4 4.9.3** - парсинг HTML
- **html5lib 1.1** - парсер HTML
- **Selenium 4.7.2** - автоматизация браузера
- **fake-useragent 0.1.11** - генерация User-Agent

### Обработка данных
- **xlwt 1.3.0** - создание Excel файлов (.xls)
- **CSV** - экспорт в CSV формат
- **scikit-learn** - машинное обучение (кластеризация)
- **pandas** - обработка данных
- **distance** - вычисление расстояний между строками

### Frontend
- **HTML5**
- **jQuery**
- **Chosen** - стилизация select-элементов
- **Fancybox** - лайтбокс
- **Font Awesome** - иконки
- **Swiper** - слайдер

---

## 📁 Структура проекта

```
parser/
├── parser_app/                 # Основное приложение Flask
│   ├── __init__.py            # Инициализация приложения, настройка БД, админки
│   ├── main.py                # Основные маршруты и логика парсинга
│   ├── auth.py                # Маршруты аутентификации (login/logout/signup)
│   ├── parser_forms.py        # Формы для ввода данных
│   ├── parser_utils.py        # Утилиты для экспорта (CSV, XLS)
│   ├── resultobj.py           # Классы результатов парсинга (ParsingResult, SingleResult)
│   ├── xmlriver.py            # Работа с XML API Яндекс и Google
│   ├── request_parser.py      # Нативный парсер через requests
│   ├── request_parser_with_ya_xml.py  # Парсер через Яндекс XML
│   ├── parse_google_geo.py    # Парсинг Google с гео-привязкой
│   ├── for_cache.py           # Декоратор кэширования с таймаутом (12 часов)
│   ├── my_threads.py          # Многопоточная обработка
│   ├── utils.py               # Вспомогательные функции
│   ├── regions.py             # Регионы для парсинга
│   ├── doclusters.py          # Кластеризация на основе ML
│   ├── simpleculsters.py      # Простая кластеризация по пересечениям
│   ├── models/
│   │   └── user_models.py     # Модели пользователей и ролей (User, Role)
│   ├── templates/             # HTML-шаблоны
│   │   ├── index_2.html       # Главная страница парсера
│   │   ├── base.html          # Базовый шаблон
│   │   ├── profile.html       # Профиль пользователя
│   │   ├── subscribe.html     # Страница подписки
│   │   ├── clusters.html      # Страница кластеризации
│   │   ├── forms/             # Формы входа/регистрации
│   │   └── admin/             # Шаблоны админки
│   ├── lrs.txt                # Список регионов Яндекс
│   ├── geo.txt                # Список регионов Google
│   └── db.sqlite              # База данных SQLite
├── venv38_flask/              # Виртуальное окружение Python 3.8
├── passenger_wsgi.py          # Точка входа для WSGI (Beget)
├── results.csv                # Результаты экспорта CSV
├── results.xls                # Результаты экспорта XLS
└── requirements.txt           # Зависимости Python
```

---

## ⚙️ Логика работы

### Входные данные (форма на странице)

| Элемент | ID | Описание |
|---------|-----|----------|
| Ключевые слова | `keys` | Список запросов для проверки (максимум 50 фраз, каждая с новой строки) |
| Поисковая система | `engine` | Выбор системы: Яндекс XML (river), Яндекс XML (официальный), Google JS, Google XML |
| Регион | `region_y_chosen` / `region-g` | Географический регион для проверки выдачи |
| Количество итераций | `iteration` | Число повторений проверки (1-3) для усреднения позиций |
| Глубина проверки | `depth-*` | ТОП-10 (1 страница), ТОП-20 (2 страницы), ТОП-30 (3 страницы) |
| Кеширование | `docache_id` | Если активен, повторяющиеся запросы берутся из кэша |

### Процесс парсинга

1. **Ввод данных**: Пользователь вводит ключевые слова в поле `id="keys"`
2. **Выбор поисковика**: В `id="engine"` выбирается Яндекс или Google
3. **Выбор региона**: В `id="region_y_chosen"` указывается регион проверки
4. **Настройка итераций**: В `id="iteration"` задаётся количество проверок (для усреднения)
5. **Глубина парсинга**: 
   - `id="depth-10"` — первая страница выдачи (ТОП-10)
   - `id="depth-20"` — первые две страницы (ТОП-20)
   - `id="depth-30"` — первые три страницы (ТОП-30)
6. **Кеширование**: При активном `id="docache_id"` повторные запросы не выполняются, а берутся из кэша
   - **Время жизни кэша**: 12 часов (настроено в `for_cache.py`)
7. **Запуск**: Клик по кнопке `class="parser-submit"` запускает парсинг

### Доступные движки парсинга

| Код | Движок | Описание |
|-----|--------|----------|
| 11 | Native Yandex | Нативный парсинг через requests |
| 12 | Yandex XML River | Через XML API (xmlriver) |
| 13 | Yandex XML Official | Официальный Яндекс XML |
| 21 | Google JS | Парсинг через Selenium |
| 22 | Google XML | Через XML API |

---

## 🔧 Функционал

### 1. Парсинг поисковой выдачи
- Поддержка Яндекс и Google
- Выбор региона парсинга
- Множественные итерации для усреднения результатов
- Глубина сканирования до 30 позиций

### 2. Кеширование запросов
- Автоматическое сохранение результатов
- Время хранения: **12 часов** (настраивается в `for_cache.py`)
- Экономия лимитов пользователя при повторных запросах
- Реализовано через декоратор `@timed_lru_cache` в `for_cache.py`
- ⚠️ **Проблема**: Кеш хранится в оперативной памяти процесса, сбрасывается при перезапуске WSGI/приложения

### 3. Кластеризация запросов
- Активация через форму `id="cluster-form"`
- Поле `class="klastery_percent"` для указания процента пересечений URL
- **Логика**: 
  - При 30% — таблицы объединяются в кластер, если 30% URL совпадают
  - При 50% — требуется 50% совпадений URL
- Алгоритм основан на расчёте пересечений множеств URL между запросами
- Реализовано в `resultobj.py` (метод `calc_clusters`)
- ⚠️ **Известные проблемы**:
  1. **Дублирование вызова**: Кластеризация выполняется дважды — автоматически после парсинга (строка 104 в `main.py`) и повторно по кнопке "Кластеризовать"
  2. **Асимметричный расчет**: Формула `len(A∩B) / len(A) × 100%` не учитывает размер множества B, что приводит к некорректным результатам при разной длине выдач
  3. **Рекомендация**: Использовать индекс Жаккара: `|A∩B| / |A∪B| × 100%` для симметричного расчета

### 4. Экспорт данных
- **CSV**: Кнопка `class="download-csv"` → скачивание `results.csv`
- **XLS**: Кнопка `class="download-xls"` → скачивание `results.xls`
- Формирование файлов происходит в `parser_utils.py`

### 5. Копирование URL
- Клик по `data-clip="#clipboard-block-1"` копирует все URL из таблицы `class="answer-table"` в буфер обмена
- Данные для копирования находятся в скрытом блоке `id="clipboard-block-{номер}"`

### 6. Система пользователей
- Регистрация и авторизация
- Роли: **User**, **Admin**
- Лимиты запросов для пользователей
- Админ-панель для управления пользователями

---

## 👥 Учётные записи

### Тестовые пользователи (создаются автоматически при первом запуске)

| Роль | Email | Пароль | Лимит |
|------|-------|--------|-------|
| Admin | admin@parser.ru | qwerty1# | 100 |
| User | user@parser.ru | 123456 | стандартный |

*Данные настроены в `parser_app/__init__.py`*

---

## 🚀 Установка и запуск

### Требования
- Python 3.8
- Apache + mod_wsgi (для production на Beget)
- SQLite (встроен в Python)

### Локальная разработка

```bash
# Перейти в директорию проекта
cd /workspace/parser

# Активировать виртуальное окружение
source venv38_flask/bin/activate  # Linux/Mac
# или
venv38_flask\Scripts\activate  # Windows

# Установить зависимости
pip install -r parser_app/requirements.txt

# Дополнительные пакеты (не в requirements.txt)
pip install flask flask-sqlalchemy flask-login flask-admin flask-security xlwt selenium scikit-learn pandas distance

# Запустить приложение
python -m flask --app parser_app run --debug
```

### Production (Beget)

1. Разместить файлы на сервере Beget
2. Настроить `passenger_wsgi.py` с правильными путями:
   ```python
   sys.path.append('/home/r/rapcooc5/mklines/public_html/parser/parser_app')
   sys.path.append('/home/r/rapcooc5/mklines/public_html/parser/venv38_flask/lib/python3.8/site-packages')
   ```
3. Приложение автоматически инициализируется через:
   ```python
   application = parser_app.create_app()
   ```

---

## 📊 База данных

### Таблицы

**users** — пользователи
- id, username, email, password_hash
- user_limit (лимит запросов)
- user_wallet, user_data
- created_on, updated_on
- active (статус)

**roles** — роли
- id, name, description

**roles_users** — связь многие-ко-многим

---

## 🔐 Безопасность

- Хеширование паролей (SHA-256 через Werkzeug)
- Система ролей (Flask-Security)
- Защита маршрутов через `@login_required`
- Админ-панель доступна только пользователям с ролью Admin

---

## 📝 Конфигурация

### Основные настройки (`parser_app/__init__.py`)
```python
app.config['SECRET_KEY'] = 'dfjndsf*(&^*&3424kdflksdfds78465fdsf398439802sdfsdf'
app.config['SQLALCHEMY_DATABASE_URI'] = 'sqlite:///db.sqlite'
app.config['FLASK_ADMIN_SWATCH'] = 'cerulean'
```

### API ключи для XML (`parser_app/xmlriver.py`)
```python
USER_ID = "3089"
KEY = "9305a49e48a27d38f87261f26a6346f4d6508b6d"
```

---

## 📦 Зависимости

### Основные (parser_app/requirements.txt)
```
beautifulsoup4==4.9.3
requests==2.25.1
html5lib==1.1
fake-useragent==0.1.11
```

### Дополнительные (установлены в venv)
```
Flask==2.2.2
Flask-SQLAlchemy==3.0.2
Flask-Login==0.6.2
Flask-Admin==1.6.0
Flask-Security==3.0.0
xlwt==1.3.0
selenium==4.7.2
scikit-learn
pandas
passlib==1.7.4
```

---

## 🎯 Особенности реализации

### Кластеризация
Алгоритм кластеризации в `resultobj.py`:
1. Для каждого запроса формируется множество URL
2. Вычисляется процент пересечения между множествами
3. Запросы с пересечением ≥ барьера объединяются в кластер
4. Каждому кластеру присваивается уникальный цвет

### Кэширование
Декоратор `@timed_lru_cache` в `for_cache.py`:
- Время жизни: 12 часов (43200 секунд)
- Максимальный размер: 9512 записей
- Автоматическая очистка по истечении времени

### Многопоточность
Модуль `my_threads.py` реализует параллельный парсинг для ускорения обработки больших списков запросов.

---

## 📞 Контакты

**Компания**: MKlines  
**Сайт**: [https://mklines.ru](https://mklines.ru)  
**Инструмент**: [https://mklines.ru/parser/index](https://mklines.ru/parser/index)

---

## 📄 Лицензия

Проприетарное программное обеспечение компании MKlines.

---

## 🐛 Найденные ошибки и проблемы

### 1. Критические ошибки безопасности

#### 🔴 Утечка учетных данных XMLRiver
**Файл**: `parser_app/xmlriver.py`, строки 7-8
```python
USER_ID = "3089"
KEY = "9305a49e48a27d38f87261f26a6346f4d6508b6d"
```
**Проблема**: API-ключи хранятся в открытом виде в коде
**Решение**: Вынести в переменные окружения или файл конфигурации `.env`

#### 🔴 Hardcoded SECRET_KEY
**Файл**: `parser_app/__init__.py`, строка 67
```python
app.config['SECRET_KEY'] = 'dfjndsf*(&^*&3424kdflksdfds78465fdsf398439802sdfsdf'
```
**Проблема**: Секретный ключ Flask засвечен в репозитории
**Решение**: Генерировать случайный ключ и хранить в переменной окружения

#### 🔴 Отключение проверки CAPTCHA
**Файл**: `parser_app/request_parser.py`, строки 19-21, 35-37
```python
current_user.limits = current_user.limits - 1
db.session.commit()
```
**Проблема**: Лимиты списываются ДО успешного выполнения запроса, даже если получена CAPTCHA
**Решение**: Списывать лимиты только после успешного парсинга без ошибок

### 2. Ошибки логики

#### 🟡 Дублирование кластеризации
**Файл**: `parser_app/main.py`, строки 104 и 69-78
```python
# Строка 104 - автоматический вызов
result_of_parsing.calc_clusters(barrier)

# Стройки 69-78 - повторный вызов по кнопке
if request.form.get("klastery-inp") and result_of_parsing:
    result_of_parsing.calc_clusters(barrier)
```
**Проблема**: Кластеризация выполняется дважды при каждом парсинге
**Решение**: Убрать автоматический вызов (строка 104), оставить только по кнопке

#### 🟡 Асимметричный расчет кластеризации
**Файл**: `parser_app/resultobj.py`, строка 69
```python
bar = len(data[k1] & data[k2]) / len(data[k1]) * 100
```
**Проблема**: Формула не симметрична относительно k1 и k2
- Пример: Если A={1,2,3}, B={1,2}, то пересечение = {1,2}
- Для A→B: 2/3 × 100% = 66.7%
- Для B→A: 2/2 × 100% = 100%
**Решение**: Использовать индекс Жаккара: `len(A∩B) / len(A∪B) × 100%`

#### 🟡 Глобальные переменные
**Файл**: `parser_app/main.py`, строки 27-33
```python
in_search = None
search = None
region = None
engine = None
depth = None
iteration = None
result_of_parsing = ['error']
```
**Проблема**: Глобальное состояние между запросами пользователей может привести к утечке данных
**Решение**: Хранить состояние в сессии пользователя или базе данных

### 3. Проблемы производительности

#### 🟠 Отсутствие пагинации результатов
**Проблема**: При большом количестве запросов страница становится огромной
**Решение**: Добавить пагинацию или AJAX-подгрузку таблиц

#### 🟠 Синхронный парсинг
**Файл**: `parser_app/request_parser.py`, метод `run_parse`
**Проблема**: Запросы выполняются последовательно, нет асинхронности
**Решение**: Использовать `asyncio` или Celery для фоновых задач

#### 🟠 Неэффективная работа с БД
**Файл**: `parser_app/request_parser.py`, строки 19-21
```python
current_user.limits = current_user.limits - 1
db.session.commit()  # commit после КАЖДОГО запроса
```
**Проблема**: Частые коммиты замедляют работу
**Решение**: Группировать обновления и делать commit пакетом

### 4. Устаревшие зависимости

| Пакет | Версия в проекте | Актуальная версия | Статус |
|-------|-----------------|-------------------|--------|
| requests | 2.25.1 | 2.31.0+ | ⚠️ Устарел |
| beautifulsoup4 | 4.9.3 | 4.12.0+ | ⚠️ Устарел |
| fake-useragent | 0.1.11 | 1.4.0+ | ⚠️ Устарел |
| urllib3 | 1.26.2 | 2.0.0+ | ⚠️ Устарел |
| Flask | ? | 3.0.0+ | ❓ Не указан |

**Рекомендация**: Обновить зависимости с тестированием функционала

### 5. Проблемы архитектуры

#### 🔸 Смешивание PHP и Python
**Файлы**: `index.php` + Flask приложение
**Проблема**: Непонятное разделение логики между PHP и Python
**Решение**: Мигрировать полностью на Flask или четко разделить ответственность

#### 🔸 Отсутствие обработки ошибок API
**Файл**: `parser_app/xmlriver.py`
**Проблема**: Нет обработки таймаутов, сетевых ошибок, лимитов API
**Решение**: Добавить retry-логику с экспоненциальной задержкой

#### 🔸 Файлы результатов в общей директории
**Файлы**: `results.csv`, `results.xls`
**Проблема**: Пользователи могут перезаписывать файлы друг друга
**Решение**: Использовать уникальные имена файлов с ID пользователя/сессии

---

## ✅ Рекомендации по оптимизации

### Краткосрочные (быстрая польза)

1. **Исправить расчет кластеризации**
   ```python
   # Было:
   bar = len(data[k1] & data[k2]) / len(data[k1]) * 100
   
   # Стало (индекс Жаккара):
   bar = len(data[k1] & data[k2]) / len(data[k1] | data[k2]) * 100
   ```

2. **Убрать дублирование кластеризации**
   - Удалить строку 104 в `main.py`: `result_of_parsing.calc_clusters(barrier)`

3. **Перенести секреты в .env**
   ```bash
   # .env файл
   XMLRIVER_USER=3089
   XMLRIVER_KEY=9305a49e48a27d38f87261f26a6346f4d6508b6d
   SECRET_KEY=случайная_строка_64_символа
   ```

4. **Исправить списание лимитов**
   - Переместить decrement лимитов после успешного парсинга

### Долгосрочные (архитектурные улучшения)

1. **Внедрить Celery/RQ** для фоновых задач парсинга
2. **Добавить WebSocket** для отображения прогресса парсинга в реальном времени
3. **Миграция на PostgreSQL** для лучшей производительности
4. **REST API** для интеграции с другими сервисами
5. **Docker-контейнеризация** для упрощения деплоя
6. **Unit-тесты** для критической логики (парсинг, кластеризация)

---

## 📊 Структура базы данных

### Таблица `users`
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Primary key |
| username | String(64) | Уникальное имя |
| email | String(120) | Уникальный email |
| password_hash | String(128) | Хэш пароля (SHA256) |
| user_limit | Integer | Общий лимит запросов |
| limits | Integer | Текущий остаток лимитов |
| user_wallet | Integer | Баланс кошелька |
| user_data | String(256) | Дополнительные данные |
| created_on | DateTime | Дата создания |
| updated_on | DateTime | Дата обновления |

### Таблица `roles`
| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer | Primary key |
| name | String(80) | Название роли (User/Admin) |
| description | String(255) | Описание роли |

### Таблица `roles_users` (многие-ко-многим)
| Поле | Тип | Описание |
|------|-----|----------|
| user_id | Integer | FK → users.id |
| role_id | Integer | FK → roles.id |

---

## 📞 Контакты

**Компания**: MKlines  
**Сайт**: https://mklines.ru  
**Инструмент**: https://mklines.ru/parser/index

---

*Документ обновлен: Декабрь 2024*  
*Версия документации: 2.0*
